import React from 'react';

const Show = ({ name, ingredients, technique, course }) => {

    // console.log('Display Props:', name, ingredients, technique, course)

    return (
    
     <div>
            <h3>{name}</h3>
            </div>

    )
}


export default Show;